'use strict'
let chai = require('chai');
let assert = chai.assert;
let expect = chai.expect;
require('mocha-sinon');

describe('Load and read a document', () => {

			describe('Return correct string when passed numeric value', () => {
				beforeEach(function () {
						//sinon.spy(obj, "func");
					};

					afterEach(function () {
							//teardown
						};

						it("Should return file from path", function () {
							//Loader.load("/some/resource");
							//assert(Loader.parseURI.calledOnce);
						});

					});

			});